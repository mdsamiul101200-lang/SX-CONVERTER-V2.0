import {spawn} from "node:child_process";const p=spawn("node",["server/server.js"],{env:{...process.env,PORT:"8099"},stdio:"inherit"});try{for(let i=0;i<30;i++){try{const r=await fetch("http://127.0.0.1:8099/api/health");if(r.ok){console.log("health OK");break}}catch{}await new Promise(r=>setTimeout(r,300))}console.log(await (await fetch("http://127.0.0.1:8099/api/formats")).text())}finally{p.kill("SIGTERM")}